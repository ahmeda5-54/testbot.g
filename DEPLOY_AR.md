# دليل التشغيل والبيع (GitHub → Railway)

هذا الدليل خاص بعمليّة تقديم النسخة لمشترٍ جديد: أنت ترفع النسخة النهائية
إلى GitHub، وكل مشترٍ يستلمها على **حسابه هو** ويضع مفاتيحه الخاصة من
المتصفح — **دون أن يرى مفاتيحك** ودون أن تشارك حسابك على أي موقع استضافة.

---

## 1) رفع النسخة إلى GitHub (أنت — مرة واحدة)

1. أنشئ مستودعاً جديداً على GitHub.
2. ارفع المشروع إليه (المجلد الحالي) — الملفات الحساسة **مستثناة تلقائياً**:
   - `.env` (مفاتيحك)، `data/` (قاعدة البيانات)، `uploads/` (الصور)، `userbot/` (الجلسات)
   - هذان الاستثناءان موجودان أصلاً في `.gitignore` و`.dockerignore`.
3. تأكد من رفع: `Dockerfile`, `railway.json`, `Procfile`, `requirements.txt`, `run.py`.

> ⚠️ قبل الرفع تأكد أن `.env` لم يُرفع للتاريخ أبداً — لو سبق رفعه
> (git history) غيّر التوكنات فوراً، لأن التاريخ يبقى محفوظاً.

## 2) تسليم المشتري (كل مشترٍ — حساب خاص به)

أرسل له رابط المستودع + هذه الخطوات:

1. ادخل إلى **github.com** وأنشئ حساباً إن لم يملك (مجاني).
2. افتح **railway.app** وسجّل دخول بـ GitHub (حسابه هو).
3. **New Project → Deploy from GitHub repo** → اختر المستودع.
4. Railway يقرأ `railway.json` تلقائياً ويشغّل التطبيق — لا يحتاج تعديل أي ملف.

## 3) وضع المفاتيح — من المتصفح (الطريقة الجديدة)

لا حاجة لملفات ولا لمتغيرات:

1. افتح رابط اللوحة الذي يوفره Railway (صفحة `Settings → Networking → Generate Domain`).
2. أولَ فتحة سيوجّهك النظام تلقائياً إلى صفحة **«معالج الإعداد الأول»**.
3. يملأ فيها المشتري **مفاتيحه هو**: توكن البوت (من @BotFather)، معرف القناة،
   معرفات الأدمن، كلمة مرور اللوحة، بوت الدعم اختيارياً... ثم «حفظ وتشغيل».
4. تُخزن كلها في **قاعدة بيانات مشروعه** (وليس في أي ملفات أو كود)، ويُعاد
   تشغيل البوت تلقائياً بمفاتيحه الجديدة.

لتعديل المفاتيح لاحقاً: داخل اللوحة → الإعدادات والمتابعة → «⚙️ معالج الإعداد الأول».

> البديل الاحترافي لمن يفضّل: متغيرات Railway (`Variables`) — مشفّرة عند
> التخزين ولا تظهر في الكود إطلاقاً. المعالج يتولّى نفس الشيء تلقائياً.

## 4) 🔑 نظام الترخيص (تجريبي / دائمي + كود تفعيل)

طريقة البيع لكل مشترٍ من نفس الرابط:

1. **أنت (البائع)** تضيف في متغيرات Railway الخاصة بمشروعك: `LICENSE_SECRET=<سر عشوائي طويل>`.
   هذا السر يفعّل نظام الأكواد — بدونه يكون المشروع في «وضع المالك» ويعمل بلا قيود.
2. **أنت (البائع)** تولّد رمزاً لكل مشترٍ من جهازك:
   ```
   python tools/make_license.py --kind trial --days 30 --members 60 --customer "Ali"
   python tools/make_license.py --kind permanent --members 5000 --customer "Buyer"
   ```
   (يتطلب `LICENSE_SECRET` نفسه مضبوطاً في `.env` عندك — أضفه هناك أيضاً).
3. أرسل للمشتري **رابط اللوحة + الكود**.
4. أول ما يفتح الرابط تظهر شاشة: **نسخة تجريبية / نسخة دائمة** — يختار نوع
   نسخته ويدخل الكود.
5. تحقّق النظام → النسخة تفعّل → ينتقل المشتري لمعالج الإعداد ويضع مفاتيحه → يشتغل.
6. النسخة التجريبية: حد زمني (**ينتهي** ويُطلب كود جديد) + **سقف أعضاء** يمنع
   التسجيل فوقه، مع شارة «نسخة تجريبية» في اللوحة وتنبيهات دورية للأدمن عبر تلغرام.
7. **بيع جديد بنفس الرابط**: من الإعدادات → بطاقة «حالة الترخيص» →
   زر «🗑️ إعادة تهيئة لعميل جديد» يمسح كل البيانات والترخيص، ويعود الرابط
   لشاشة التفعيل لمشترٍ جديد.

## 5) ⚠️ أهم خطوة: لا تضيّع بياناتك عند إعادة النشر

خزّان Railway **مؤقت** (يُمحى مع كل re-deploy)، ومشروعنا يخزن في SQLite:
- من لوحة Railway: افتح مشروعك → **Volumes** → **New Volume** (سعة 1GB مجانية).
- اربطه بالمسار **`/app/data`** (نقطة تركيب الـ Volume).
- بهذا تبقى قاعدة البيانات والصور والجلسات محفوظة عبر كل تحديث.
- ملاحظة: `LICENSE_SECRET` والبيئة تُضاف من `Variables` (تبقى محفوظة بلا Volume).

## 6) نصيحة بيع

- النسخة التجريبية: يسجّل المشتري بمفاتيحه الخاصة خلال دقائق — لا حاجة لنسخة خاصة.
- السر `LICENSE_SECRET` لا يُظهر في الكود إطلاقاً وليس في قاعدة البيانات —
  حماية الأكواد من التوليد من خارج متغيرات الاستضافة.